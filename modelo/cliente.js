/*const mongoose = require('mongoose');
//const shema=mongoose.shema()
const { Schema } = mongoose;
const clienteshe = new Schema({
    documento: String,
    nombre: String,
    apellido: String,
    correo: String,
    sexo: String,
    fechanacimiento: String,
    celular: String,
})
const cliente = mongoose.model('cliente', clienteshe)
module.exports = cliente;*/
